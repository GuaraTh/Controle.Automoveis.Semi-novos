{
  "name": "controle-automoveis-semi-novos",
  "version": "1.0.0",
  "description": "Aplicação para controle de automóveis semi-novos",
  "scripts": {
    "install:all": "npm run install:backend",
    "install:backend": "cd backend && npm install",
    "dev": "cd backend && npm run dev",
    "start": "cd backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}